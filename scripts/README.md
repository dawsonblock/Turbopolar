# Scripts

- `smoke_test.py` — append tokens to a cache and print telemetry.
- `bench_cache_compression.py` — print cache compression ratio across sequence lengths.
- `check_promotion_gate.py` — synthetic promotion gate smoke check.

All scripts are best-effort diagnostics, not official benchmarks.
