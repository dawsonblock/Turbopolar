"""TurboPolar benchmark and promotion orchestration scripts.

This package exposes the core orchestration functions so they can be
imported and tested without relying on sys.path manipulation or running
the scripts as top-level processes.

CLI entry points are thin wrappers that call the importable functions.
"""
